{
  "name": "sumate-ya",
  "version": "0.1.0",
  "private": true,
  "description": "Sumate Ya - Football player connection platform monorepo",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "sumate-ya": "ln -sfn ../../.env apps/backend/.env && echo 'Symlinked root .env into apps/backend/.env. Frontend keeps its own optional apps/frontend/.env'",
    "kill:ports": "bash scripts/kill-dev-ports.sh",
    "predev": "echo 'Skipping predev'",
    "dev": "turbo dev",
    "predev:frontend": "bash scripts/kill-dev-ports.sh 4321",
    "dev:frontend": "turbo dev --filter=@sumate-ya/frontend",
    "predev:backend": "bash scripts/kill-dev-ports.sh 4000",
    "dev:backend": "turbo dev --filter=@sumate-ya/backend",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck --force",
    "test": "turbo test",
    "pretest:e2e": "bash scripts/kill-dev-ports.sh",
    "test:e2e": "npm --prefix apps/testing test",
    "pretest:e2e:headed": "bash scripts/kill-dev-ports.sh",
    "test:e2e:headed": "npm --prefix apps/testing run test:headed",
    "codegen": "turbo codegen",
    "format": "turbo format",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f app"
  },
  "devDependencies": {
    "supabase": "^2.89.1",
    "turbo": "^2.8.20"
  }
}
